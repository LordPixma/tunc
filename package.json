{
  "name": "tunc-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@slack/webhook": "^7.0.5"
  },
  "scripts": {
    "test": "vitest run --exclude \"frontend/**\" && npm --prefix frontend test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250806.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
